[project]
name = "cs336-lg-scaling-laws"
version = "0.1.0"
description = "Public IsoFLOPs scaling-law fitting module for the CS336-LG portfolio project."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10",
    "numpy>=2.0",
]

[tool.uv]
package = false
